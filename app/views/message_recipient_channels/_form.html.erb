<%= form_for(@message_recipient_channel) do |f| %>
  <% if @message_recipient_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_recipient_channel.errors.count, "error") %> prohibited this message_recipient_channel from being saved:</h2>

      <ul>
      <% @message_recipient_channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message_id %><br>
    <%= f.text_field :message_id %>
  </div>
  <div class="field">
    <%= f.label :channel_id %><br>
    <%= f.text_field :channel_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
